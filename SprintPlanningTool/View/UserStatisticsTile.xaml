<UserControl x:Class="SprintPlanningTool.View.UserStatisticsTile"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:local="clr-namespace:SprintPlanningTool.View"
             BorderBrush="White"
             BorderThickness="1"
             mc:Ignorable="d" >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding TicketsDoneText}" Grid.Row="0"></TextBlock>
        <TextBlock Text="{Binding BugsDoneText}" Grid.Row="1"></TextBlock>
        <TextBlock Foreground="DarkOrange" Text="{Binding StoryPointsDoneText}" Grid.Row="2"></TextBlock>
        <TextBlock Foreground="DarkOrange" Text="{Binding LinesOfCodeWrittenText}" Grid.Row="3"></TextBlock>
        <TextBlock HorizontalAlignment="Right" Text="{Binding Username}" Grid.Row="4"></TextBlock>
    </Grid>
</UserControl>
